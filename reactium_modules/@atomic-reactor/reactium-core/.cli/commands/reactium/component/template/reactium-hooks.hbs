/**
 * -----------------------------------------------------------------------------
 * Reactium Plugin {{name}}
 * -----------------------------------------------------------------------------
 */
(async () => {
    const { Plugin, Component } = await import('@atomic-reactor/reactium-core/sdk');
    const { {{name}} } = await import('./index');
    
    await Plugin.register('{{name}}');
    Component.register('{{name}}', {{name}});
})();
